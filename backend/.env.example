# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB Connection String
# Choose ONE of the following options:

# Option 1: Local MongoDB (No Authentication - Default)
# MONGODB_URI=mongodb://localhost:27017/e2ee-messaging

# Option 2: Local MongoDB (With Authentication)
# MONGODB_URI=mongodb://username:password@localhost:27017/e2ee-messaging

# Option 3: MongoDB Atlas (Cloud - Requires Credentials)
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/e2ee-messaging?retryWrites=true&w=majority

# Default (for local development without auth):
MONGODB_URI=mongodb://localhost:27017/e2ee-messaging

# Frontend URL
FRONTEND_URL=http://localhost:5173

# JWT Secret (for future use)
JWT_SECRET=your-secret-key-change-this-in-production
